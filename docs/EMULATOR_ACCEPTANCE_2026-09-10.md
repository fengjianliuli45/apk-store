# 模拟器验收追加记录（2026-09-10）

基准：`7847f87`；设备：本机 `Stopwatch_API_35`，API 35 / x86_64。

## 本轮发现与修复

强制结束进程后，训练草稿恢复到第 2/10 组且处于安全暂停态，但 Flutter 降级训练页只显示“完成这组”，没有恢复入口。新增“已暂停”和“继续训练”，点击后显示 3 秒倒计时，结束后恢复计时与完成组入口。暂停期间不显示完成组按钮。

## 已验证

- 新增组件回归：暂停状态无完成入口，点击继续触发倒计时，恢复后计时增长。
- Flutter 全量 115 项测试通过；静态检查无问题。
- 通用 Debug APK 构建、覆盖安装成功。
- 模拟器实测强制结束并重启，原第 2/10 组仍可恢复；新入口恢复后计时增长至 00:11.75。
- 输入重量、隐藏键盘、保存本组进入休息态；结束并保存后返回首页 READY/开始训练，进程存活。末次日志抽查未见致命异常或先前控制器红屏。
- 截图位于工作区 `.codex_tmp/acceptance-resume-fixed.png`、`.codex_tmp/acceptance-rest.png`。
- APK：`flutter/build/app/outputs/flutter-apk/app-debug.apk`；SHA-256：`D5A938DE28A8BC1F56E87865E09472C57A345115FAB83DE0730193995980C504`。此前文档中的 APK 哈希是历史构建。

## 未完成与下一步

- 远程推送：代理端口不可用；临时禁用代理直连仍发生连接重置/超时，远端提交状态未能确认。未更改全局代理配置，网络恢复后需重新查询并推送。
- 本轮完成的是恢复缺陷修复及相关回归，不是全部验收。连续整课十组、疼痛/RIR/末组恢复输入、锁屏/后台、Activity 重建和重复快速操作矩阵仍需补齐。
- ARM64 真机 Unity、30 分钟稳定性、真实后端认证与跨设备冲突仍待验收。
- 本次代码修复回滚基准为 `7847f87`，通过撤销本次修复提交回滚，避免覆盖其他变更。
