# Stopwatch 项目交接说明（2026-08-27，2026-08-28 换机补充）

## 当前主线

- 仓库：`fengjianliuli45/apk-store`
- 分支：`main`
- 正式客户端：`flutter/`（Flutter + Unity 双平台方案；Android P0 已完成 API 35 模拟器功能验收，等待 ARM64 真机与发布阻塞清理）
- 规划引擎权威源：`fitness-planner/`
- 产品决策入口：[`INDEX.md`](INDEX.md)

## 本次交接包含

- `FZ-01`～`FZ-06` 首发冻结决策与统一产品设计草案。
- 阶段目标、阶段达成判定和用户训练响应画像的 Python/Dart 同步实现。
- Flutter 规划网关对应模型与测试。
- 训练首页视觉网页原型及 QA 证据（仅供设计追溯，不代表正式 App 已改版）。
- App 内嵌 3D 技术路线已确认：Flutter 主壳 + Unity as a Library 全屏训练 + 可选 GLB 轻量预览，详见 `EMBEDDED_3D_RUNTIME_SOLUTION_2026-08-27.md`。
- Android Phase A 已生成真实 Unity Android Library：Unity Personal 许可证已由 Editor 接受，Activity 入口版 Library 已成功导出并同步到本机 `flutter/android/unityLibrary`；全屏 Host Activity、首指令排队、返回、卸载、条件 Manifest 和 ARM64 Debug APK 构建已完成，真机运行联调待完成。
- 首页与导航栏讨论已暂停；以 2026-08-27 会话确认图片为阶段视觉基调，遗留问题见 `PRODUCT_DESIGN_SPEC_V1.md` 第 13 节。

## 2026-09-02 计划前端 v3 实施状态

本节记录当前本地工作区的最新状态，优先于下方旧的测试数量与模拟器限制描述；尚未声明已推送远端。

- 已合并 `main@6b4739b` 的规划引擎 1.8、动作数据和对应 Dart 测试，同时保留现有 Flutter/Unity 会话实现。
- 计划页已实现阶段/中周期、RIR、容量、减载、主项负荷、引擎版本和同步状态；阶段复评改为确定性引擎结论，不再回到规划问卷。
- 已实现 append-only 本地计划版本、旧版本恢复、离线优先保存，以及符合后端 OpenAPI 的 `POST /api/v1/plans` 客户端。真实云同步仍需 API 地址、认证令牌和运行中的后端联合验收。
- 聚合训练日志已适配为引擎可识别的出勤证据。模拟器复评确认 13% 完成率对应“出勤未达标（1/20）”，不会再误报 0/20；未采集的重量、RIR、恢复和疼痛数据不伪造。
- Android App 层 APK 现在同时支持 `arm64-v8a` 和 `x86_64`，Flutter 壳可在 x86_64 模拟器启动。Unity Library 仍只有 ARM64；Host 已增加 ABI 可用性保护，重新构建后 x86_64 会把 Unity 判定为不可用而不进入 `UnityPlayerActivity`。保护代码已由独立 Kotlin 编译验证，但本轮完整 APK 重建/重装因本机 Gradle 缓存目录权限审批超时未完成；Unity 功能验收仍以 ARM64 真机/镜像为准。
- 旧本地计划迁移元数据已修正：主计划与历史“当前版本”统一显示引擎 1.8。
- 2026-09-02 最终验证：`flutter analyze` 无问题；Flutter 全量 105 项测试通过；Android 15 / API 35 x86_64 模拟器完成计划概览、阶段复评和版本历史视觉验收，无溢出。
- 本轮修改前回滚材料：`.codex-backups/frontend-v3-20260902/`，包含原 `flutter/lib`、`flutter/test`、pubspec、设计规范和 Android App Gradle 文件；模拟器验收截图也保存在该目录。

本方向的下一步：先补齐逐组重量、次数、RIR、疼痛和恢复的结构化训练证据，并完成真实后端认证/同步联调；随后用能产生 `advance`、`extend`、`deload_then_retry` 和 `address_safety` 的四组证据分别验收复评文案、计划变化和版本恢复。

## 2026-08-30 最新验收结论

详细矩阵、证据、APK 指纹和发布阻塞见 [`ACCEPTANCE_2026-08-30.md`](ACCEPTANCE_2026-08-30.md)。本节结论优先于下方 2026-08-29 的历史问题记录。

- Android P0 训练闭环已在 Android 15 / API 35 模拟器通过：准备、开始、手动计次、暂停/3 秒继续、休息、加 30 秒、跳过、系统返回、进程恢复、暖复用、完整完成和中途结束均已验证。
- 完整 10 组 / 120 次训练完成后自动回首页 `READY`，不再出现“再次训练”。
- SQLite 已升级到 v3，保存精确当前组计时与完整计划队列；完成会话为 `completed`，中途结束为 `stopped` 并保存 `incomplete` 组。
- 二次进入导致共享进程 `SIGKILL`、Back、输入、休息日虚假训练、页面溢出、缺失 Play Asset Delivery 类和暖 Runtime 新会话序号问题已修复。
- Flutter 全量 34 项测试通过，`flutter analyze` 0 问题；Unity Library Build Report 和最终 APK 构建均成功，最终日志无致命异常。
- 最终 APK：`flutter/build/app/outputs/flutter-apk/app-debug.apk`，`167,064,391` bytes，SHA-256 `EB7ABF8F14878266FAE15D9EFA7C6354AF46FF52F65BEB7C80A871089C2496B0`。
- 当前发布阻塞不是 Android P0 业务代码：Unity `trial version` 水印、ARM64 真机矩阵、iOS 接入、女性教练/配音/宠物与跨设备同步仍未完成。资产授权已于 2026-08-30 澄清：Mixamo 无需另购授权；Neural4D 只需确认付费档并归档生成记录。
- 本轮代码修改前回滚点：`.codex-backups/p0-acceptance-before-20260829/`；验收证据：`.codex_tmp/acceptance-20260829/`。

## 新设备启动

```powershell
git clone https://github.com/fengjianliuli45/apk-store.git
cd apk-store\flutter
flutter pub get
flutter test test/stage_goal_test.dart
flutter run
```

如需验证 Python 规划引擎：

```powershell
cd apk-store\fitness-planner
python -m unittest discover -s tests -p "test_*.py" -v
```

## 本次验证结果

- Python：104 项测试通过。
- Flutter：`test/stage_goal_test.dart` 4 项测试通过。
- Dart 静态分析（`lib/planner` 与阶段目标测试）：无错误；`plan_sync.dart` 有 4 个既有 `unnecessary_cast` 警告。
- `git diff --check`：通过。
- Android Phase A 基础实现后：Flutter 全量 27 项测试通过；新增/修改文件定向分析 0 问题。
- Unity Runtime 与 Editor 程序集 `dotnet build` 均为 0 错误，仅有 6 个既有序列化字段 CS0649 警告。
- Android Debug APK 完整 Gradle 构建通过：273 个任务成功，Unity IL2CPP ARM64 编译成功。产物 `flutter/build/app/outputs/apk/debug/app-debug.apk` 为 132.82 MiB，SHA-256 `CBECBDC9C436907D9FE640D9BF3F1C735A52C84191243B085478BDE8F8ABFC84`，V2 调试签名验证通过。
- 合并 Manifest 验证：仅 `MainActivity` 一个 Launcher；`StopwatchUnityActivity` 存在；生成的 `UnityPlayerActivity` Launcher 已移除；`unity.splash-enable=False`、`extractNativeLibs=true`、`minSdk=26`。APK 只包含 `arm64-v8a`，并包含 `libflutter.so`、`libunity.so`、`libil2cpp.so` 与 `libmain.so`。
- Unity `6000.5.4f1` 图形 Editor 导出成功：Activity 入口版 Library Build Report 为 Success，生成物约 426.7 MB；已确认 `UnityPlayerActivity`、`UnityPlayer.UnitySendMessage`、`unityLibrary/build.gradle` 和 ARM64 `libunity.so` 存在。

## 2026-08-28 真机联调结果与当前阻塞

- 测试手机：荣耀 `AAP-AN00`，USB 序列号 `A7EB6R5C31001615`。
- Windows 已正确枚举荣耀 USB 复合设备 `VID_339B/PID_107D`，荣耀复合设备驱动 `oem132.inf` 和 `Android Composite ADB Interface` 驱动 `oem128.inf` 均为 `Started`。
- VMware Workstation、运行中的虚拟机和 VMware USB Arbitration Service 已检查。手机仍由 Windows 主机驱动接管，主机 ADB 能读取设备序列号，因此**不是虚拟机独占 USB**。
- 荣耀手机助理 `11.0.0.724` 已通过 HDB 成功连接手机并显示联系人、信息、图片和存储数据；该连接只证明 HDB/线缆/驱动可用。手机助理中的手机画面是静态连接示意图，不是可控制的实时投屏，不能代替用户确认 Android RSA 调试授权。
- 发现荣耀手机助理启动的 ADB server 为版本 39，而 Android SDK platform-tools 客户端为版本 41；两者争用本机 `5037` 端口。SDK 曾明确输出 `adb server version (39) doesn't match this client (41)`。手机助理进程已临时关闭，SDK ADB 41 已重新启动。
- 排除版本冲突后，设备仍为 `unauthorized`。Android 安全模型要求在已解锁的手机屏幕上确认“允许 USB 调试”RSA 指纹，电脑端、HDB 和手机助理均不能代点。
- **本轮没有安装 APK，也没有获得任何真机 Unity 运行证据。** 冷启动、Unity Logo/闪白、进入训练、系统返回、卸载、二次进入、后台恢复、动作循环、接地、材质和性能仍全部待另一台机器验证。
- 当前 SDK ADB：`1.0.41 / 37.0.0-14910828`。若另一台机器同时运行荣耀手机助理，测试前应完全退出手机助理，再执行 `adb kill-server`、`adb start-server`，避免 39/41 端口冲突。

## 换机所需代码与资产

### A. Git 克隆应提供的内容

从 `fengjianliuli45/apk-store` 的 `main` 分支取得 Flutter 主应用、Android Host/Bridge、同步脚本和文档。克隆后先确认以下文件存在：

- `flutter/lib/unity/`：Flutter 协议、Bridge 和会话协调器。
- `flutter/android/app/src/unity/`：条件 Unity Manifest 与 `StopwatchUnityActivity.kt`。
- `tools/sync_unity_android_library.ps1`：将 Unity Activity 导出同步到 Flutter Android 工程。
- `docs/EMBEDDED_3D_RUNTIME_SOLUTION_2026-08-27.md`：架构、生命周期和验收标准。
- `docs/MALE_COACH_ASSET_AUDIT_2026-08-27.md`：角色和动作资产审计、SHA-256 与发布限制。

以下内容被 `.gitignore` 排除，**不能假定 Git 克隆后存在**：

- `flutter/android/unityLibrary/`
- `flutter/android/shared/`
- `flutter/android/unity-export.properties`
- `unity-export/`
- `flutter/build/` 和 `*.apk`
- `.codex-backups/`、`.codex_tmp/`、Unity/Gradle/Flutter 构建缓存

### B. 可重建 Unity 的必需资产包

本机正式 Unity 工程不在 `apk-store` 快照内，也不属于单独 Git 工作树，路径为：

`D:\Codex_pro\Stopwatch\repo-main\stopwatch-main\unity\StopwatchUnity`

换机时至少完整复制以下三个目录，必须保留所有 `.meta` 文件：

| 目录 | 当前规模 | 必需性 | 说明 |
| --- | ---: | --- | --- |
| `Assets/` | 214 文件，216.65 MiB | 必需 | 场景、脚本、男性模型、动作、材质、贴图、Motion Catalog 和 Android 导出器。 |
| `Packages/` | 2 文件 | 必需 | 包清单与锁文件；包含 `com.unity.modules.androidjni`。 |
| `ProjectSettings/` | 27 文件，约 0.06 MiB | 必需 | Unity 版本、Android、IL2CPP、Splash/Logo 和 Player 设置。 |

不要复制 `Library/`、`Temp/`、`Logs/`、`obj/` 作为权威源；这些目录约占当前 2.8 GiB 工程的大部分，应让新机器重建。`UserSettings/` 仅为个人编辑器偏好，可不交接。

Unity 工程中的 P0 关键资产：

- `Assets/Models/Coach/StopwatchMaleCoach_MixamoRigged_v2.fbx` 及其 `.meta`：正式 65 骨男性教练，禁止回退到旧 23 骨版本。
- `Assets/Animations/Coach/bodyweight_squat.fbx` 及其 `.meta`。
- `Assets/Animations/Coach/push_up.fbx` 及其 `.meta`。
- `Assets/Models/Coach/` 内的基础色、金属度、粗糙度和 JPG/PNG 贴图。
- `Assets/Resources/StopwatchExerciseMotionCatalog.asset` 及其 `.meta`。
- `Assets/Scenes/SquatTraining.unity` 及其 `.meta`。
- `Assets/Scripts/`、`Assets/Editor/ExerciseMotionCatalogBuilder.cs`、`Assets/Editor/StopwatchAndroidLibraryExporter.cs`。

### C. 原始男性教练源资产包

需要继续调整蒙皮、骨骼或重新导出 FBX 时，再单独复制：

`D:\Codex_pro\Stopwatch\repo-main\stopwatch-main\assets\body-model\mixamo\male-coach-v1`

当前约 103.31 MiB，包含 Blender 源文件、原始 Mixamo Rigged FBX、深蹲测试动作和验证截图。仅做现有 Unity 工程构建时，Unity `Assets/` 内的正式副本已经足够；该原始包用于可编辑资产追溯。

权威文件 SHA-256：

- 正式男性模型：`F445E1A4D5B37D1B966709D943756322541E831251654085ACB05D5D5106BF67`
- 深蹲动作：`AD13BF4DEC339C68B31A72203EA37C8293557EADC55C8ED12DA211878FBC46ED`
- 俯卧撑动作：`F1C4C271EA41454381853751FF35F742C3837F68B00DCC21B9B5B385FFAD9EC6`

男性模型由用户用 Neural4D 自生成，动作来自 Mixamo 免费库。Adobe Mixamo FAQ 允许免版税嵌入商业游戏成品，禁止转售原始 FBX，无需另购授权。Neural4D 付费订阅生成可商用；免费档仅限自用。此前「商业授权未归档」改为建议保存订阅/生成页与条款快照，不再作为另购授权硬阻塞。女性教练资产仍不存在，不是本次换机阻塞项。

### D. 快速真机验证产物

若另一台机器只负责安装和验收，不需要先重建 Unity，可单独复制：

`D:\Codex_pro\Stopwatch\apk-store\flutter\build\app\outputs\apk\debug\app-debug.apk`

- 大小：`139,273,959` bytes（132.82 MiB）
- SHA-256：`CBECBDC9C436907D9FE640D9BF3F1C735A52C84191243B085478BDE8F8ABFC84`
- 架构：仅 `arm64-v8a`
- Android：`minSdk 26`（Android 8.0+）
- 签名：V2 Debug 签名

该 APK 被 Git 忽略，必须通过移动硬盘、局域网或其他受控方式单独传输。安装前先用 SHA-256 校验完整性。

### E. 可选的已导出 Library 与回滚材料

若希望跳过新机器上的首次 Unity 导出，可复制以下可再生目录：

- `apk-store/unity-export/AndroidExportActivity/`：Activity 入口版 Unity 导出；整个 `unity-export/` 当前约 853.46 MiB。
- `apk-store/flutter/android/unityLibrary/` 与 `flutter/android/shared/`：已同步模块；当前 `unityLibrary/` 连同本机构建产物约 1.64 GiB。
- `apk-store/flutter/android/unity-export.properties`：本机路径相关属性。复制到新机器后必须修改 `unityProjectPath`、SDK/JDK/NDK 路径，不得原样信任绝对路径。

推荐仍以 Unity 源工程重新导出并运行同步脚本；上述导出目录只作为节省时间的可选缓存，不是权威源。

本机回滚材料位于：

- `.codex-backups/unity-host-before-20260827/`
- `.codex-backups/unity-sync-before-20260828/`
- `.codex-backups/gradle-proxy-before-20260828/`

这些目录同样不进入 Git。若需要在新机器复现当前回滚能力，应一并复制；否则以新的 Git 分支或提交建立换机后的回滚点。

## 换机工具链基线

- Unity：`6000.5.4f1`，revision `d550df8bd089`；安装 Android Build Support、SDK/NDK Tools、OpenJDK，并在新机器激活 Unity Personal。
- Flutter：`3.47.0`，revision `4cf24164269a5ebf0c16a028a00727d0e77bbb05`。
- Dart：`3.13.0`。
- Java：Unity 随附 OpenJDK `17.0.18`。
- Android SDK：Platform 36、Build Tools 36.0.0。
- Unity 导出属性：`compileSdk=36`、`targetSdk=36`、`minSdk=26`、`arm64-v8a`、Unity 自带 NDK `27.2.12479018`。
- Flutter 完整 Gradle 构建本机另安装 NDK `28.2.13676358`；Gradle Wrapper `9.3.1`。
- 检查新机器全局 Gradle 配置，不得保留指向未监听端口的 `systemProp.http.proxyHost` / `systemProp.https.proxyHost`。本机曾被失效的 `127.0.0.1:10808` 阻塞。

## 另一台机器的最短继续步骤

1. 克隆 `apk-store`，立即创建个人工作分支或本地提交作为回滚点。
2. 快速验收优先复制并校验上述 Debug APK；完全退出荣耀手机助理、Android Studio 的旧 ADB 和其他手机管理工具，只保留一套 SDK platform-tools。
3. 手机保持解锁，开启 USB 调试并确认 RSA 指纹；执行 `adb devices -l`，状态必须是 `device`，不能是 `unauthorized`。
4. 执行：

   ```powershell
   adb install -r .\app-debug.apk
   adb shell am force-stop com.restpod.hud
   adb logcat -c
   adb shell am start -n com.restpod.hud/.MainActivity
   ```

5. 按本文“继续工作的顺序”第 4 项和 `EMBEDDED_3D_RUNTIME_SOLUTION_2026-08-27.md` 第 12 节完成真机测试矩阵，保存截图、录屏、`adb logcat`、设备型号/API/内存数据。
6. 若需改 Unity：复制 Unity 源工程三目录，使用完全一致的 Unity 版本打开并激活 Personal；先验证 Humanoid/动作/材质，再导出 `AndroidExportActivity`，运行 `tools/sync_unity_android_library.ps1`，最后重新构建 Flutter APK。

## 2026-08-29 模拟器实测记录

本机已在 Android 15 / API 35 / x86_64 模拟器 `Stopwatch_API_35` 上安装现有 ARM64-only Debug APK。该 Google 系统镜像能通过 ARM 转译层启动 APK，但这不等价于 ARM64 真机的兼容性或性能结论。

已验证：

- APK 安装成功，Flutter 冷启动成功，首屏未观察到 Unity Logo。
- 调试手机号 `13800000000` + OTP `123456` 可完成本地登录和首次规划流程。
- 从 Flutter 训练舱进入 `StopwatchUnityActivity` 成功，`unitySurfaceView` 和男性教练模型正常渲染，可从准备态进入训练态。
- 将 App 切到后台后重新从 Launcher 打开，Flutter 训练舱可恢复并显示“3D 教练已连接”。
- 结束流程能回到 Flutter 总结页，再返回首页。

发现的 P0/P1 问题：

1. Unity 画面右下角持续显示 `trial version` 水印。原因尚未定位，发布前必须消除并在重建 APK 后复验。
2. 系统 Back 键在 Unity Activity 内无反应，未返回 Flutter。
3. Unity “点击计次”按钮重复点击后仍为 `0/1`；长按“结束训练”也未在 Unity 内生效。需检查 Unity 输入与 Flutter 状态机的事件桥接。
4. 休息日首页仍可进入一个名为“休息日”的 `1 组 × 1 次` Unity 训练，与“按计划恢复”语义冲突。
5. 训练总结页在 1080×2400 模拟设备上出现 `BOTTOM OVERFLOWED BY 6.6 PIXELS`。
6. 结束后再次触发训练时，Flutter 停留在启动画面，随后模拟器进程退出。模拟器日志未留下明确 Android `FATAL EXCEPTION`，且 ARM 转译 + SwiftShader 期间 RenderThread 增至约 30；暂按“模拟器/转译层不稳定，同时二次进入生命周期未通过”处理，必须用 ARM64 真机判定。
7. Unity 日志记录了缺失 `com.google.android.play.core.assetpacks.AssetPackManager` 的 `ClassNotFoundException`，未导致首次渲染失败，但应清理无用的 Play Asset Delivery 引用或补齐依赖。

证据保存在 `apk-store/.codex_tmp/emulator-test-20260829/`，包含冷启动、Flutter 首页、Unity 首帧/训练态/计次、前后台、总结页截图及 logcat。该目录是本机测试证据，不作为 Git 权威源。

为尝试原生 x86_64 Unity 导出所做的 Exporter/Gradle 改动已全部回滚，回滚后三个文件的 SHA-256 与试验前备份一致。备份保留在 `.codex-backups/unity-emulator-x64-before-20260829/`。

## 继续工作的顺序

> 2026-08-30 更新：下列早期排查顺序保留用于追溯；当前实际下一步以 [`ACCEPTANCE_2026-08-30.md`](ACCEPTANCE_2026-08-30.md) 第 7 节为准。

1. 男性教练资产已在本机核实，结论与文件指纹见 [`MALE_COACH_ASSET_AUDIT_2026-08-27.md`](MALE_COACH_ASSET_AUDIT_2026-08-27.md)。资产可用于 P0 工程验证。授权口径见该文档 2026-08-30 澄清：不再把「缺少商业授权」列为发布硬阻塞；Unity 真机复验仍待完成。
2. 女性教练采用审计文档第 5 节的准入门槛；在来源授权和同骨架复用验证完成前，不采购、不生成、不绑定首发。
3. Unity `6000.5.4f1` Personal 许可证、Android JNI 模块和 Activity 入口版 Library 导出已完成；仍需在 Editor/真机复验 Humanoid Avatar、深蹲/俯卧撑完整循环、接地和材质。
4. Library 同步脚本会复制 `unityLibrary/`、`shared/` 和根级 Unity Gradle 属性；Android Host、Bridge、状态持久化与暖 Runtime 生命周期已通过 API 35 模拟器功能验收。**下一步先处理 `trial version` 水印，再在 ARM64 真机执行完整生命周期/性能矩阵。** 资产侧只需确认 Neural4D 付费档并归档记录，不必另买 Mixamo/模型授权。
5. Gradle Plugin Portal 阻塞已解除，根因是共享 Gradle 配置残留了未监听的 `127.0.0.1:10808` 代理；移除后插件和依赖解析成功。修改前配置保存在 `.codex-backups/gradle-proxy-before-20260828/`。
6. 首页与导航栏问题保持暂停，除非产品负责人明确恢复讨论。
7. Flutter/Unity 正式界面不得直接照搬网页原型或阶段截图，需在设计恢复并确认后实施。

本轮 Host 修改的本地回滚点位于 `.codex-backups/unity-host-before-20260827/`，可运行其中的 `rollback.ps1` 一键恢复；该目录已加入 `.gitignore`，不会进入交付仓库。

## 外部设计

- Figma：<https://www.figma.com/design/aOErCB953ojiETVuTqYuNR>

Figma 文件是可编辑的首轮训练首页稿；当前会话确认图片仍是阶段视觉基调，二者均未使首页与导航栏达到最终验收状态。
